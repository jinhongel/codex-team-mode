interface:
  display_name: "Team Mode"
  short_description: "Route focused subagents only when they add clear value"
  default_prompt: "Use $team-mode for this task. Delegate only when the expected benefit clearly exceeds coordination cost; keep unresolved decisions and final acceptance in the main thread."

policy:
  allow_implicit_invocation: true
